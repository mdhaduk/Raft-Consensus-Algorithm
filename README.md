# Raft-Consensus-Algorithm
This project involves building a complete implementation of the Raft consensus algorithm, a distributed consensus protocol designed for managing replicated state machines.

# Assignment 1

Building basic infrastructure layer for now. 
-> frontend service that spawns server processes
-> basic server that responds to gRPC health checks

raft.proto defines gRPC structure 

